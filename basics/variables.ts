let age = "eighteen";

//not allowed because implicitly type is set to string
// age = 18;

let newAge: any = "eighteen";

//alowed because type is set to any
newAge = 18;

//primitive types are - number, boolean, string
